import { SiteHeader } from "@/components/SiteHeader"; import { SiteFooter } from "@/components/SiteFooter"; import { ProjectPortal } from "@/components/ProjectPortal"; import { projects } from "@/data/projects";
export default function Work(){return <><SiteHeader/><main><section className="page-hero"><p className="eyebrow">Selected work / 2019—2026</p><h1>Work with a point<br/>of <em>view.</em></h1><p>A selective archive of websites, product experiences and frontends shaped for clarity, confidence and real use.</p></section><section className="page-content"><div className="work-list">{projects.map((p,i)=><ProjectPortal key={p.slug} project={p} index={i}/>)}</div></section></main><SiteFooter/></>}
