"use client";
import { FormEvent, useState } from "react";
import { ArrowUpRight } from "lucide-react";
import { SiteHeader } from "@/components/SiteHeader";
import { SiteFooter } from "@/components/SiteFooter";

export default function Contact(){const[submitted,setSubmitted]=useState(false);function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);const body=[`Name: ${data.get("name")}`,`Agency / company: ${data.get("company")}`,`Email: ${data.get("email")}`,`Support: ${data.get("support")}`,"",`Brief: ${data.get("brief")}`].join("\n");window.location.href=`mailto:usman.khairdin@gmail.com?subject=${encodeURIComponent("New paid-pilot enquiry")}&body=${encodeURIComponent(body)}`;setSubmitted(true)}return <><SiteHeader/><main><section className="contact-layout route-contact"><div><p className="eyebrow">Start a project</p><h1>Bring a clear brief. We’ll find the right next step.</h1><p>A focused paid pilot is a simple way to assess quality and working fit before expanding the relationship.</p><p className="contact-direct">Or write directly to <a href="mailto:usman.khairdin@gmail.com">usman.khairdin@gmail.com</a></p></div><form className="brief-form" onSubmit={submit}>{submitted&&<p className="form-success">Your email app is opening with the brief prepared.</p>}<label>Name<input required name="name" placeholder="Your name"/></label><label>Agency / company<input required name="company" placeholder="Company name"/></label><label>Work email<input required type="email" name="email" placeholder="you@company.com"/></label><label>What support do you need?<select name="support"><option>UI design</option><option>Frontend implementation</option><option>Design + frontend</option><option>White-label overflow support</option></select></label><label>Brief, scope or link<textarea name="brief" placeholder="Tell me a little about the work, timing and what you need."/></label><button className="button button-dark" type="submit">Prepare the brief <ArrowUpRight size={17}/></button><p className="form-note">This opens a prepared email in your default email app; you stay in control of sending it.</p></form></section></main><SiteFooter/></>}
